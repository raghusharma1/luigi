# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=_check_parser_a1d81ddd5b
ROOST_METHOD_SIG_HASH=_check_parser_326832c82e


Scenario 1: Test the scenario when parser_class is not enabled
Details:
  TestName: test_parser_class_not_enabled
  Description: This test is intended to verify the function's ability to correctly raise an ImportError when the parser_class is not enabled.
Execution:
  Arrange: Create an instance of LuigiConfigParser or LuigiTomlParser with the enabled attribute set to False.
  Act: Call the function _check_parser with the created parser_class instance and the parser name as parameters.
  Assert: Check if an ImportError is raised with the expected error message.
Validation:
  This test ensures that the function correctly handles the scenario where the parser class is not enabled, which is a key part of the method's business logic.

Scenario 2: Test the scenario when parser_class is enabled
Details:
  TestName: test_parser_class_enabled
  Description: This test is intended to verify the function's ability to not raise an exception when the parser_class is enabled.
Execution:
  Arrange: Create an instance of LuigiConfigParser or LuigiTomlParser with the enabled attribute set to True.
  Act: Call the function _check_parser with the created parser_class instance and the parser name as parameters.
  Assert: Check if no exception is raised.
Validation:
  This test ensures that the function correctly handles the scenario where the parser class is enabled, which is a fundamental part of the method's business logic.

Scenario 3: Test the scenario when parser_class is None
Details:
  TestName: test_parser_class_none
  Description: This test is intended to verify the function's behavior when parser_class is None.
Execution:
  Arrange: Prepare to pass None as parser_class.
  Act: Call the function _check_parser with None as the parser_class and the parser name as parameters.
  Assert: Check if an AttributeError is raised, as the function will attempt to access the 'enabled' attribute of None.
Validation:
  This test ensures that the function handles None inputs for parser_class as expected, raising an AttributeError. This is important as it verifies the function's robustness against invalid inputs.
"""

# ********RoostGPT********
import logging
import os
import warnings
import pytest
from cfg_parser import LuigiConfigParser
from toml_parser import LuigiTomlParser
from core import _check_parser

class Test_CoreCheckParser:
  
    def test_parser_class_not_enabled(self):
        # Arrange
        parser_class = LuigiConfigParser()
        parser_class.enabled = False
        parser = 'cfg'
        
        # Act and Assert
        with pytest.raises(ImportError) as excinfo:
            _check_parser(parser_class, parser)
        assert "Parser not installed yet. Please, install luigi with required parser:\npip install luigi[cfg]" in str(excinfo.value)
        
    def test_parser_class_enabled(self):
        # Arrange
        parser_class = LuigiConfigParser()
        parser_class.enabled = True
        parser = 'cfg'
        
        # Act and Assert
        try:
            _check_parser(parser_class, parser)
        except Exception as e:
            pytest.fail(f"_check_parser raised {type(e).__name__} unexpectedly!")
            
    def test_parser_class_none(self):
        # Arrange
        parser_class = None
        parser = 'cfg'
        
        # Act and Assert
        with pytest.raises(AttributeError) as excinfo:
            _check_parser(parser_class, parser)
        assert "'NoneType' object has no attribute 'enabled'" in str(excinfo.value)
