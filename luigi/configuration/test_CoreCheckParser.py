# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=_check_parser_a1d81ddd5b
ROOST_METHOD_SIG_HASH=_check_parser_326832c82e


```plaintext
Scenario 1: Testing when the Parser is Installed
Details:
  TestName: test_check_parser_when_installed
  Description: This test verifies that the function _check_parser works as expected when the parser_class is enabled (i.e., the Parser that is being checked is installed).
Execution:
  Arrange: Initialize parser_class with a mock object where 'enabled' returns True.
  Act: Invoke the _check_parser function with the initialized parser_class and a parser name as parameters.
  Assert: The function should not raise any error.
Validation:
  If a Parser is installed ('enabled' is True), _check_parser will not raise an exception. This verifies that the method behaves as expected for valid installed Parsers and contributes to the error-free functioning of the program.

Scenario 2: Testing when the Parser is Not Installed
Details:
  TestName: test_check_parser_when_not_installed
  Description: This test ensures that _check_parser correctly raises an ImportError when the parser_class is not enabled (i.e., the Parser that is being checked isn't installed).
Execution:
  Arrange: Set up a parser_class with a mock object where 'enabled' returns False.
  Act: Call the _check_parser function with the initialized parser_class and a parser name as parameters.
  Assert: The function should raise an ImportError with a message instructing the user on how to install the parser.
Validation:
  It is important that the system correctly handles scenarios where required modules (i.e. Parsers) aren't installed. The system should fail safely by raising an ImportError with clear instructions for the user on how to fix the issue.

Scenario 3: Testing the Error Message for Non-Installed Parser
Details:
  TestName: test_error_message_for_non_installed_parser
  Description: This test checks that the error message given when the parser is not installed is correct and informative.
Execution:
  Arrange: Create a parser_class with a mock object where 'enabled' returns False.
  Act: Invoke the _check_parser function with the prepared parser_class and parser name as parameters.
  Assert: Verify that an ImportError is raised and that the exception message contains the phrase "Parser not installed yet. Please, install luigi with required parser: pip install luigi[{parser}]". Replace {parser} with the name of the parser passed to the function.
Validation:
  It is important that errors generated by the system are descriptive to help users diagnose the issue. By checking that the error message contains the correct instructions for installing the missing parser, we ensure that users can quickly resolve the issue and that the function serves as a helpful diagnostic tool.
```
"""

# ********RoostGPT********
import pytest
import logging
import os
import warnings
from cfg_parser import LuigiConfigParser
from toml_parser import LuigiTomlParser
from unittest.mock import Mock

# Assume that the function _check_parser is imported from the core module
from core import _check_parser 

class Test_CoreCheckParser:
    # Test when the Parser is installed
    def test_check_parser_when_installed(self):
        # Arrange
        mock_parser_class = Mock()
        mock_parser_class.enabled = True
        parser_name = "Mock_Parser"
        
        # Act & Assert
        try:
            _check_parser(mock_parser_class, parser_name)
        except Exception as e:
            pytest.fail(f"Test failed with exception {e}")
    
    # Testing when the Parser is Not Installed
    def test_check_parser_when_not_installed(self):
        # Arrange
        mock_parser_class = Mock()
        mock_parser_class.enabled = False
        parser_name = "Mock_Parser"
        
        # Act & Assert
        with pytest.raises(ImportError):
            _check_parser(mock_parser_class, parser_name)
    
    # Testing the Error Message for Non-Installed Parser
    def test_error_message_for_non_installed_parser(self):
        # Arrange
        expected_error_message = "Parser not installed yet. Please, install luigi with required parser:\npip install luigi[Mock_Parser]"
        mock_parser_class = Mock()
        mock_parser_class.enabled = False
        parser_name = "Mock_Parser"
        
        # Act & Assert
        with pytest.raises(ImportError) as exception_info:
            _check_parser(mock_parser_class, parser_name)
        
        # Check if the error message is correct
        assert str(exception_info.value) == expected_error_message
