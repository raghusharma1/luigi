# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=_interpolate_env_ea5eb159c9
ROOST_METHOD_SIG_HASH=_interpolate_env_9d7c1514cf


Scenario 1: Testing the interpolation of an existing environment variable
Details:
  TestName: test_interpolation_of_existing_env_variable
  Description: This test is intended to verify that the function correctly interpolates an environment variable that exists.
Execution:
  Arrange: Set an environment variable with a known value.
  Act: Invoke the function with a value that contains the name of the environment variable.
  Assert: Check that the function returns the value with the environment variable replaced by its value.
Validation:
  The function should be able to interpolate environment variables. This test confirms that the function correctly handles this case.

Scenario 2: Non-existing environment variable
Details:
  TestName: test_interpolation_of_non_existing_env_variable
  Description: This test is intended to verify that the function raises an exception when attempting to interpolate a non-existing environment variable.
Execution:
  Arrange: Ensure that the environment variable does not exist.
  Act: Invoke the function with a value that contains the name of the non-existent environment variable.
  Assert: Check that the function raises an InterpolationMissingEnvvarError.
Validation:
  The function should raise an error when it attempts to interpolate a non-existing environment variable. This test confirms that the function correctly handles this case.

Scenario 3: Testing interpolation of multiple environment variables
Details:
  TestName: test_interpolation_of_multiple_env_variables
  Description: This test is intended to verify that the function correctly interpolates multiple environment variables within a single value.
Execution:
  Arrange: Set multiple environment variables with known values.
  Act: Invoke the function with a value that contains the names of the environment variables.
  Assert: Check that the function returns the value with the environment variables replaced by their values.
Validation:
  The function should be able to interpolate multiple environment variables within a single value. This test confirms that the function correctly handles this case.

Scenario 4: Testing the behavior with an empty value
Details:
  TestName: test_interpolation_with_empty_value
  Description: This test is intended to verify that the function correctly handles an empty value.
Execution:
  Arrange: No special arrangement is needed.
  Act: Invoke the function with an empty value.
  Assert: Check that the function returns an empty string.
Validation:
  The function should return an empty string when invoked with an empty value. This test confirms that the function correctly handles this case.
"""

# ********RoostGPT********
import os
import pytest
from configparser import InterpolationError
from cfg_parser import _interpolate_env

# Mocking the InterpolationMissingEnvvarError
class InterpolationMissingEnvvarError(InterpolationError):
    def __init__(self, option, section, value, envvar):
        msg = (
            "Config refers to a nonexisting environment variable {}. "
            "Section [{}], option {}={}"
        ).format(envvar, section, option, value)
        InterpolationError.__init__(self, option, section, msg)

class Test_EnvironmentInterpolationInterpolateEnv:

    def test_interpolation_of_existing_env_variable(self):
        os.environ["TEST_VAR"] = "TEST_VALUE"
        result = _interpolate_env("option", "section", "${TEST_VAR}")
        assert result == "TEST_VALUE"

    def test_interpolation_of_non_existing_env_variable(self):
        with pytest.raises(InterpolationMissingEnvvarError):
            _interpolate_env("option", "section", "${NON_EXISTING_VAR}")

    def test_interpolation_of_multiple_env_variables(self):
        os.environ["TEST_VAR1"] = "TEST_VALUE1"
        os.environ["TEST_VAR2"] = "TEST_VALUE2"
        result = _interpolate_env("option", "section", "${TEST_VAR1}${TEST_VAR2}")
        assert result == "TEST_VALUE1TEST_VALUE2"

    def test_interpolation_with_empty_value(self):
        result = _interpolate_env("option", "section", "")
        assert result == ""
