# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=_interpolate_env_ea5eb159c9
ROOST_METHOD_SIG_HASH=_interpolate_env_9d7c1514cf


```
Scenario 1: Test for successful interpolation of environment variable
Details:
  TestName: test_interpolation_success
  Description: This test is intended to verify that the function _interpolate_env correctly interpolates a valid environment variable.
Execution:
  Arrange: Prepare the environment by setting an environment variable. Initialize an instance of the class with appropriate interpolations.
  Act: Invoke the _interpolate_env function, passing the name of the environment variable as a parameter.
  Assert: Check that the returned value matches the value of the environment variable.
Validation:
  This test ensures that the function correctly interpolates an existing environment variable, which is central to its business logic.

Scenario 2: Test for unsuccessful interpolation due to non-existing environment variable
Details:
  TestName: test_interpolation_failure
  Description: This test is intended to verify that the function _interpolate_env raises an InterpolationMissingEnvvarError when attempting to interpolate a non-existing environment variable.
Execution:
  Arrange: Initialize an instance of the class with appropriate interpolations.
  Act: Invoke the _interpolate_env function, passing the name of a non-existing environment variable as a parameter.
  Assert: Check that an InterpolationMissingEnvvarError is raised.
Validation:
  This test ensures that the function correctly handles the scenario where the environment variable to be interpolated does not exist, which is an important edge case in its business logic.

Scenario 3: Test for successful interpolation of multiple environment variables
Details:
  TestName: test_multiple_interpolation_success
  Description: This test is intended to verify that the function _interpolate_env correctly interpolates multiple valid environment variables.
Execution:
  Arrange: Prepare the environment by setting multiple environment variables. Initialize an instance of the class with appropriate interpolations.
  Act: Invoke the _interpolate_env function, passing a string containing the names of the environment variables as a parameter.
  Assert: Check that the returned value matches the concatenated values of the environment variables.
Validation:
  This test ensures that the function correctly handles multiple environment variables, which is a complex scenario in its business logic.

Scenario 4: Test for unsuccessful interpolation due to one non-existing environment variable among multiple environment variables
Details:
  TestName: test_multiple_interpolation_failure
  Description: This test is intended to verify that the function _interpolate_env raises an InterpolationMissingEnvvarError when attempting to interpolate multiple environment variables and one of them does not exist.
Execution:
  Arrange: Prepare the environment by setting multiple environment variables. Initialize an instance of the class with appropriate interpolations.
  Act: Invoke the _interpolate_env function, passing a string containing the names of the environment variables, one of which does not exist, as a parameter.
  Assert: Check that an InterpolationMissingEnvvarError is raised.
Validation:
  This test ensures that the function correctly handles the scenario where one of the environment variables to be interpolated does not exist, which is a complex edge case in its business logic.
```
"""

# ********RoostGPT********
import os
import pytest
from configparser import InterpolationMissingEnvvarError
from cfg_parser import _interpolate_env

class Test_EnvironmentInterpolationInterpolateEnv:
    @pytest.mark.positive
    def test_interpolation_success(self):
        os.environ['TEST_ENV_VAR'] = '123'
        result = _interpolate_env('TEST_ENV_VAR', 'section', 'value')
        assert result == '123', "Interpolation of environment variable failed"

    @pytest.mark.negative
    def test_interpolation_failure(self):
        with pytest.raises(InterpolationMissingEnvvarError):
            _interpolate_env('NON_EXISTING_ENV_VAR', 'section', 'value')

    @pytest.mark.positive
    def test_multiple_interpolation_success(self):
        os.environ['TEST_ENV_VAR1'] = '123'
        os.environ['TEST_ENV_VAR2'] = '456'
        result = _interpolate_env('TEST_ENV_VAR1TEST_ENV_VAR2', 'section', 'value')
        assert result == '123456', "Interpolation of multiple environment variables failed"

    @pytest.mark.negative
    def test_multiple_interpolation_failure(self):
        os.environ['TEST_ENV_VAR1'] = '123'
        with pytest.raises(InterpolationMissingEnvvarError):
            _interpolate_env('TEST_ENV_VAR1NON_EXISTING_ENV_VAR', 'section', 'value')
