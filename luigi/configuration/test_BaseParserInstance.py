# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=instance_61b9fedd1c
ROOST_METHOD_SIG_HASH=instance_04bd9e671f


```
Scenario 1: Test Singleton Initialization
Details:
  TestName: test_singleton_initialization
  Description: This test is intended to verify that the singleton instance gets initialized as expected when it is called for the first time.
Execution:
  Arrange: Instantiate the class with appropriate arguments.
  Act: Invoke the instance method.
  Assert: Verify that the instance is not None, and that it is the same as the one stored in cls._instance.
Validation:
  This test validates the core functionality of the singleton pattern, which guarantees that only a single instance of the singleton class exists.

Scenario 2: Test Singleton Reuse
Details:
  TestName: test_singleton_reuse
  Description: This test verifies that the instance method reuses already created instance on the subsequent calls.
Execution:
  Arrange: Instantiate the singleton class twice using the instance method.
  Act: Invoke the instance method twice and save the results in two different variables.
  Assert: Compare the two variables. They should point to the same instance.
Validation:
  This test ensures that the singleton pattern behaves as it should, reusing an existing instance, which is the core requirement of the singleton pattern.

Scenario 3: Test Logging 
Details:
  TestName: test_logging_on_instance_creation
  Description: This test verifies that a log message gets generated when an instance is loaded.
Execution:
  Arrange: Mock the logging.getLogger('luigi-interface').info method.
  Act: Invoke the instance method.
  Assert: Check whether the mock method was called with arguments 'Loaded %r', loaded.
Validation:
  Logging is a crucial aspect of any application to trace the state and behavior. This test ensures that the application generates appropriate log messages upon initialization of singleton instance.

Scenario 4: Data Reload Test
Details:
  TestName: test_data_reload
  Description: This test verifies whether the data is getting reloaded as expected when the instance method is invoked.
Execution:
  Arrange: Mock the reload function and set the return value.
  Act: Invoke the instance method.
  Assert: Assert that the mock reload method was called and it returned the expected data.
Validation:
  This test validates whether the 'reload' method is working as expected. This is important because it ensures that the instance carries the most recent and correct data.
```
"""

# ********RoostGPT********
import logging
from base_parser import BaseParser
import pytest
from unittest.mock import patch, MagicMock

class Test_BaseParserInstance:
    
    def teardown_method(self, method):
        # This method is called after each test method is executed.
        # So, set _instance as None after each test
        BaseParser._instance = None 

    @pytest.mark.regression
    def test_singleton_initialization(self):
        instance = BaseParser.instance()
        assert instance is not None    # instance is not None
        assert instance == BaseParser._instance  # it is same as stored in _instance

    @pytest.mark.regression
    def test_singleton_reuse(self):
        first_instance = BaseParser.instance()
        second_instance = BaseParser.instance()
        # Both instances are actually the same
        assert second_instance == first_instance  

    @pytest.mark.regression
    @patch('logging.getLogger')
    def test_logging_on_instance_creation(self, mock_getLogger):
        mock_logger = MagicMock()
        mock_getLogger.return_value = mock_logger
        loaded = BaseParser.instance().reload()
        mock_logger.info.assert_called_once_with('Loaded %r', loaded)

    @pytest.mark.regression
    @patch.object(BaseParser, 'reload')
    def test_data_reload(self, mock_reload):
        mock_reload.return_value = 'mock_data' 
        BaseParser.instance()
        # Check if the reload method is called
        mock_reload.assert_called()
