# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=instance_61b9fedd1c
ROOST_METHOD_SIG_HASH=instance_04bd9e671f


```python
Scenario 1: Test Singleton Creation
Details:
  TestName: test_singleton_creation
  Description: This test is intended to verify that the function correctly creates a singleton instance.
Execution:
  Arrange: Initialize cls if it does not yet have an _instance attribute, or if _instance is None.
  Act: Call instance() passing cls as the argument.
  Assert: Check that cls._instance is not None and that it is an instance of cls.
Validation:
  It validates that the method correctly instantiates a new class if no existing one is there. This is crucial because its primary focus is to provide singleton instances hence ensuring there is only one instance created at a time.

Scenario 2: Singleton Reload
Details:
  TestName: test_singleton_reload
  Description: This test validates that the singleton reload method is called when the singleton is first created and whether any possible return value gets properly logged.
Execution:
  Arrange: Prepare mock for cls and its reload method. Initialize cls._instance as None.
  Act: Call instance() passing cls as the argument.
  Assert: Assert that reload is called once for the singleton instance and that the return value gets logged.
Validation:
   Ensuring that the reload method is called and the return value is logged is important for maintaining the correct state of the singleton and for debugging and tracking purposes.

Scenario 3: Returning Existing Singleton
Details:
  TestName: test_singleton_reuse
  Description: This test is intended to verify that if a singleton already exists, the function does not create a new one but returns the existing one.
Execution:
  Arrange: Initialize cls._instance to a new instance of cls.
  Act: Call instance() passing cls as the argument.
  Assert: Check that cls._instance is still the same instance as before the function was called.
Validation:
  This verifies that anyone who calls the instance method does not create additional instances, hence adhering to the Singleton design pattern. This is important in ensuring that resources are not exhausted by unintentional multiple class instances.
```
"""

# ********RoostGPT********
import pytest
import logging
from unittest import mock
from base_parser import BaseParser

@pytest.fixture
def setup_base_parser():
    BaseParser._instance = None
    BaseParser.reload = mock.Mock()

    yield BaseParser

    del BaseParser._instance
    del BaseParser.reload

class Test_BaseParserInstance:

    @pytest.mark.regression
    def test_singleton_creation(self, setup_base_parser):
        instance = setup_base_parser.instance()
        assert instance 
        assert isinstance(instance, BaseParser)

    @pytest.mark.regression
    def test_singleton_reload(self, setup_base_parser):
        logger = logging.getLogger('luigi-interface')
        with mock.patch.object(logger, "info") as mock_info:
            setup_base_parser.reload.return_value ='Test Reload Return'
            setup_base_parser.instance()

        setup_base_parser.reload.assert_called_once()
        mock_info.assert_called_once_with('Loaded %r', 'Test Reload Return')

    @pytest.mark.regression
    def test_singleton_reuse(self, setup_base_parser):
        first_instance = setup_base_parser.instance()
        second_instance = setup_base_parser.instance()
        assert first_instance == second_instance
