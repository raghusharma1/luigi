# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=get_config_133e2e1291
ROOST_METHOD_SIG_HASH=get_config_8a95b2590c


Scenario 1: Test for default parser selection when no parser is explicitly specified.
Details:
  TestName: test_default_parser_selection
  Description: This test is intended to verify that the function get_config correctly selects and uses the default parser when none is explicitly provided.
Execution:
  Arrange: No specific arrangement needed as no parser is provided.
  Act: Invoke the get_config function without any parameters.
  Assert: Check that the returned parser is the default parser.
Validation:
  This test is important to confirm that the function can operate correctly without explicit parser specification, and thus adheres to the principles of good default management in software design.

Scenario 2: Test for correct parser selection when a specific parser is provided.
Details:
  TestName: test_specific_parser_selection
  Description: This test is intended to verify that the function get_config correctly selects and uses the specified parser when one is provided.
Execution:
  Arrange: Specify a parser that differs from the default one.
  Act: Invoke the get_config function with the specified parser as a parameter.
  Assert: Check that the returned parser is the one specified.
Validation:
  This test is important to confirm that the function can correctly interpret and act upon provided parameters, ensuring flexibility and customization for advanced users.

Scenario 3: Test for raising an exception when an unsupported parser is specified.
Details:
  TestName: test_unsupported_parser_exception
  Description: This test is intended to verify that the function get_config raises an exception when an unsupported parser is specified.
Execution:
  Arrange: Specify a parser that is not supported.
  Act: Invoke the get_config function with the unsupported parser as a parameter.
  Assert: Check that an ImportError is raised.
Validation:
  This test is important to confirm that the function correctly handles errors and edge cases, ensuring robustness and stability in real-world usage.

Scenario 4: Test for warning prompt when an invalid parser is set in the environment variable.
Details:
  TestName: test_invalid_parser_warning
  Description: This test is intended to verify that the function get_config prompts a warning when an invalid parser is set in the environment variable.
Execution:
  Arrange: Set an invalid parser in the 'LUIGI_CONFIG_PARSER' environment variable.
  Act: Invoke the get_config function.
  Assert: Check that a warning is prompted.
Validation:
  This test is important to confirm that the function can correctly handle invalid inputs from the environment variables, providing user feedback and defaulting to safe values.
"""

# ********RoostGPT********
import logging
import os
import warnings
import pytest
from cfg_parser import LuigiConfigParser
from toml_parser import LuigiTomlParser
from core import get_config

class Test_CoreGetConfig:

    def test_default_parser_selection(self):
        # Act
        config = get_config()
        # Assert
        assert isinstance(config, LuigiConfigParser), "Expected default parser is LuigiConfigParser"

    def test_specific_parser_selection(self):
        # Arrange
        specific_parser = 'toml'
        # Act
        config = get_config(specific_parser)
        # Assert
        assert isinstance(config, LuigiTomlParser), "Expected parser is LuigiTomlParser"

    def test_unsupported_parser_exception(self):
        # Arrange
        unsupported_parser = 'unsupported'
        # Act & Assert
        with pytest.raises(ImportError):
            get_config(unsupported_parser)

    def test_invalid_parser_warning(self, monkeypatch):
        # Arrange
        invalid_parser = 'invalid'
        monkeypatch.setenv('LUIGI_CONFIG_PARSER', invalid_parser)
        # Act
        with warnings.catch_warnings(record=True) as w:
            warnings.simplefilter("always")
            get_config()
            # Assert
            assert len(w) == 1
            assert issubclass(w[-1].category, UserWarning)
            assert "Invalid parser: {parser}".format(parser=invalid_parser) in str(w[-1].message)
