# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=get_3589554733
ROOST_METHOD_SIG_HASH=get_d2f3a08528


Scenario 1: Check default behavior of the function
Details:
  TestName: test_get_default_behavior
  Description: This test is intended to verify the default behavior of the function when a valid section and option are provided.
Execution:
  Arrange: Initialize the class with a valid configuration that contains at least one section and option.
  Act: Invoke the function with the section and option as parameters.
  Assert: Check if the returned value is as expected from the configuration.
Validation:
  This test is necessary to ensure that the function's basic functionality is working as intended. It serves as a baseline for other tests.

Scenario 2: Check handling of missing section
Details:
  TestName: test_get_missing_section
  Description: This test is intended to verify how the function behaves when the provided section does not exist.
Execution:
  Arrange: Initialize the class with a valid configuration that does not contain the section to be tested.
  Act: Invoke the function with the non-existent section and a valid option as parameters.
  Assert: Check if the function raises a NoSectionError.
Validation:
  This test is important to ensure that the function correctly identifies and handles cases where the section does not exist.

Scenario 3: Check handling of missing option
Details:
  TestName: test_get_missing_option
  Description: This test is intended to verify how the function behaves when a valid section is provided, but the option does not exist.
Execution:
  Arrange: Initialize the class with a valid configuration that contains the section but not the option to be tested.
  Act: Invoke the function with the section and non-existent option as parameters.
  Assert: Check if the function raises a NoOptionError.
Validation:
  This test is important to ensure that the function correctly identifies and handles cases where the option does not exist within a valid section.

Scenario 4: Check handling of default value
Details:
  TestName: test_get_default_value
  Description: This test is intended to verify how the function behaves when a default value is provided, and the section or option does not exist.
Execution:
  Arrange: Initialize the class with a valid configuration that does not contain the section or option to be tested.
  Act: Invoke the function with the non-existent section or option and a default value as parameters.
  Assert: Check if the function returns the default value.
Validation:
  This test is important to ensure that the function correctly returns the default value when the section or option does not exist.

Scenario 5: Check handling of interpolation error
Details:
  TestName: test_get_interpolation_error
  Description: This test is intended to verify how the function behaves when the configuration contains an interpolation error.
Execution:
  Arrange: Initialize the class with a configuration that contains an interpolation error.
  Act: Invoke the function with the section and option causing the interpolation error as parameters.
  Assert: Check if the function raises an InterpolationError.
Validation:
  This test is important to ensure that the function correctly identifies and handles configurations with interpolation errors.
"""

# ********RoostGPT********
import os
import re
import warnings
from configparser import ConfigParser, NoOptionError, NoSectionError, InterpolationError
from configparser import Interpolation, BasicInterpolation
from base_parser import BaseParser
import pytest
from cfg_parser import get

class Test_LuigiConfigParserGet:

    @pytest.mark.valid
    def test_get_default_behavior(self):
        config = ConfigParser()
        config.add_section('test')
        config.set('test', 'option', 'value')
        parser = LuigiConfigParser(config)
        assert parser.get('test', 'option') == 'value'
        
    @pytest.mark.invalid
    def test_get_missing_section(self):
        config = ConfigParser()
        config.add_section('test')
        parser = LuigiConfigParser(config)
        with pytest.raises(NoSectionError):
            parser.get('nonexistent', 'option')
            
    @pytest.mark.invalid
    def test_get_missing_option(self):
        config = ConfigParser()
        config.add_section('test')
        parser = LuigiConfigParser(config)
        with pytest.raises(NoOptionError):
            parser.get('test', 'nonexistent')
            
    @pytest.mark.valid
    def test_get_default_value(self):
        config = ConfigParser()
        config.add_section('test')
        parser = LuigiConfigParser(config)
        assert parser.get('nonexistent', 'nonexistent', default='default') == 'default'
        
    @pytest.mark.invalid
    def test_get_interpolation_error(self):
        config = ConfigParser()
        config.add_section('test')
        config.set('test', 'option', '%(nonexistent)s')
        parser = LuigiConfigParser(config)
        with pytest.raises(InterpolationError):
            parser.get('test', 'option')
