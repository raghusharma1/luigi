# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=get_3589554733
ROOST_METHOD_SIG_HASH=get_d2f3a08528


Scenario 1: Test section and option exist in the config
Details:
  TestName: test_get_when_section_option_exist
  Description: This test verifies that the get method retrieves the correct value when the supplied section and the option exist in the config.
Execution:
  Arrange: Initialize a ConfigParser object with a known config, where both the section and option exist.
  Act: Invoke the get method on the ConfigParser object, supplying the known section and option.
  Assert: Check that the output is as expected, i.e. the value of the supplied option from the supplied section.
Validation:
  This test is important as it verifies the primary successful use case where both the section and the option exist in the config.

Scenario 2: Test section exists but option does not
Details:
  TestName: test_get_when_option_does_not_exist
  Description: This test verifies that the get method raises the appropriate error when the supplied section exists but the option does not.
Execution:
  Arrange: Initialize a ConfigParser object with a known config, where the section exists but the option does not
  Act: Attempt to invoke the get method on the ConfigParser object, supplying the known section and unknown option. Catch any raised exceptions.
  Assert: Verify that a NoOptionError was raised.
Validation:
  This is important as it validates that the get method reacts correctly when asked for a non-existing option from a valid section, by raising the proper exception.

Scenario 3: Test section does not exist
Details:
  TestName: test_get_when_section_does_not_exist
  Description: This test verifies that the get method raises the appropriate error when the supplied section does not exist in the config.
Execution:
  Arrange: Initialize a ConfigParser object with a known config, where the supplied section does not exist
  Act: Attempt to invoke the get method on the ConfigParser object, supplying the non-existing section. Catch any raised exceptions.
  Assert: Verify that a NoSectionError was raised.
Validation:
  This test is important as it confirms that the get method correctly handles a request to retrieve a section that does not exist in the config, by raising the correct exception.

Scenario 4: Test default value is returned when section or option does not exist
Details:
  TestName: test_get_default
  Description: This test verifies that the get method returns the supplied default value when either the section or the option do not exist in the config.
Execution:
  Arrange: Initialize a ConfigParser object with a known config and a known default value.
  Act: Invoke the get method on the ConfigParser object, supplying either a non-existent section or option and the default value.
  Assert: Check that the return value is the supplied default.
Validation:
  This test is critical as it verifies that the get method correctly handles non-existent section or option by returning the default value when it's provided. This helps prevent unnecessary errors in the application, by providing a fallback.
"""

# ********RoostGPT********
import os
import pytest
import re
import warnings
from configparser import ConfigParser, NoOptionError, NoSectionError
from base_parser import BaseParser

def test_get_when_section_option_exist():
    config = ConfigParser()
    config.add_section("Section1")
    config.set("Section1", "option1", "value1")
    assert config.get("Section1", "option1") == "value1"

def test_get_when_option_does_not_exist():
    config = ConfigParser()
    config.add_section("Section1")
    with pytest.raises(NoOptionError):
        config.get("Section1", "option2")

def test_get_when_section_does_not_exist():
    config = ConfigParser()
    config.add_section("Section1")
    with pytest.raises(NoSectionError):
        config.get("Section2", "option1")

def test_get_default():
    config = ConfigParser()
    config.add_section("Section1")
    assert config.get("Section1", "option2", default="default_value") == "default_value"
