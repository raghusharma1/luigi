# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=_get_default_parser_da6898e1c5
ROOST_METHOD_SIG_HASH=_get_default_parser_61b2c6fc97


Scenario 1: Test when 'LUIGI_CONFIG_PARSER' environment variable is set and its value is a valid parser
Details:
  TestName: test_get_default_parser_with_valid_env_var
  Description: This test is intended to verify that the function _get_default_parser correctly returns the parser set in the 'LUIGI_CONFIG_PARSER' environment variable when it's a valid parser.
Execution:
  Arrange: Set the 'LUIGI_CONFIG_PARSER' environment variable to a valid parser.
  Act: Call the function _get_default_parser.
  Assert: The function should return the parser set in the 'LUIGI_CONFIG_PARSER' environment variable.
Validation:
  This test ensures that the function correctly respects the 'LUIGI_CONFIG_PARSER' environment variable setting when it's a valid parser.

Scenario 2: Test when 'LUIGI_CONFIG_PARSER' environment variable is set but its value is not a valid parser
Details:
  TestName: test_get_default_parser_with_invalid_env_var
  Description: This test is intended to verify that the function _get_default_parser correctly returns the default parser and issues a warning when the 'LUIGI_CONFIG_PARSER' environment variable is set to an invalid parser.
Execution:
  Arrange: Set the 'LUIGI_CONFIG_PARSER' environment variable to an invalid parser.
  Act: Call the function _get_default_parser.
  Assert: The function should return the default parser and a warning should be issued.
Validation:
  This test ensures that the function correctly falls back to the default parser and issues a warning when the 'LUIGI_CONFIG_PARSER' environment variable is set to an invalid parser.

Scenario 3: Test when 'LUIGI_CONFIG_PARSER' environment variable is not set
Details:
  TestName: test_get_default_parser_without_env_var
  Description: This test is intended to verify that the function _get_default_parser correctly returns the default parser when the 'LUIGI_CONFIG_PARSER' environment variable is not set.
Execution:
  Arrange: Make sure the 'LUIGI_CONFIG_PARSER' environment variable is not set.
  Act: Call the function _get_default_parser.
  Assert: The function should return the default parser.
Validation:
  This test ensures that the function correctly returns the default parser when the 'LUIGI_CONFIG_PARSER' environment variable is not set.
"""

# ********RoostGPT********
import os
import warnings
import pytest
from core import _get_default_parser
from cfg_parser import LuigiConfigParser
from toml_parser import LuigiTomlParser

class Test_CoreGetDefaultParser:

    @pytest.mark.valid
    def test_get_default_parser_with_valid_env_var(self):
        # Arrange
        os.environ['LUIGI_CONFIG_PARSER'] = 'cfg'
        expected_parser = LuigiConfigParser

        # Act
        actual_parser = _get_default_parser()

        # Assert
        assert actual_parser == expected_parser

    @pytest.mark.invalid
    def test_get_default_parser_with_invalid_env_var(self):
        # Arrange
        os.environ['LUIGI_CONFIG_PARSER'] = 'invalid_parser'
        expected_parser = LuigiConfigParser

        # Act
        with warnings.catch_warnings(record=True) as w:
            warnings.simplefilter("always")
            actual_parser = _get_default_parser()

        # Assert
        assert len(w) == 1
        assert issubclass(w[-1].category, UserWarning)
        assert str(w[-1].message) == "Invalid parser: cfg"
        assert actual_parser == expected_parser

    @pytest.mark.valid
    def test_get_default_parser_without_env_var(self):
        # Arrange
        if 'LUIGI_CONFIG_PARSER' in os.environ:
            del os.environ['LUIGI_CONFIG_PARSER']
        expected_parser = LuigiConfigParser

        # Act
        actual_parser = _get_default_parser()

        # Assert
        assert actual_parser == expected_parser
