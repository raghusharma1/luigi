# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=_get_default_parser_da6898e1c5
ROOST_METHOD_SIG_HASH=_get_default_parser_61b2c6fc97


Scenario 1: Check with the valid Luigi Config Parser
Details:
  TestName: test_with_valid_luigi_config_parser
  Description: This test is to verify the correct setting and retrieval of LuigiConfigParser.
Execution:
  Arrange: Set the environment variable 'LUIGI_CONFIG_PARSER' to 'LuigiConfigParser'.
  Act: Invoke the function _get_default_parser.
  Assert: The returned parser should be 'LuigiConfigParser'.
Validation:
  This is important to ensure that while invoking the function, the correct parser is being returned. If LuigiConfigParser has been set in the environment, then the function should return the same. Similarly, applies to other parsers.

Scenario 2: Check with an invalid parser
Details:
  TestName: test_with_invalid_parser
  Description: Tests how the function behaves when an invalid parser is set as the environment variable.
Execution:
  Arrange: Set the environment variable 'LUIGI_CONFIG_PARSER' to an invalid parser that is not in the list of PARSERS.
  Act: Invoke the function _get_default_parser.
  Assert: The function should issue a warning and return with the DEFAULT_PARSER.
Validation:
  This test assures the robustness of the system in the event an invalid parser is provided. The function is designed to default to the DEFAULT_PARSER when an invalid parser is set in the environment, which should be verified in this test.

Scenario 3: Check with the Luigi Toml Parser
Details:
  TestName: test_with_luigi_toml_parser
  Description: This test is to verify the correct setting and retrieval of LuigiTomlParser.
Execution:
  Arrange: Set the environment variable 'LUIGI_CONFIG_PARSER' to 'LuigiTomlParser'.
  Act: Invoke the function _get_default_parser.
  Assert: The returned parser should be 'LuigiTomlParser'.
Validation:
  Similar to LuigiConfigParser, if LuigiTomlParser has been set in the environment, then _get_default_parser function should return this parser. 

Scenario 4: Check when the LUIGI_CONFIG_PARSER is not set
Details:
  TestName: test_when_luigi_config_parser_is_not_set
  Description: This test is to verify that the function correctly returns the DEFAULT_PARSER when no environment variable is set for LUIGI_CONFIG_PARSER.
Execution:
  Arrange: Make sure that 'LUIGI_CONFIG_PARSER' environment variable is not set.
  Act: Invoke the function _get_default_parser.
  Assert: The returned parser should be the DEFAULT_PARSER.
Validation:
  It's important to check that the function behaves as expected when no environment variable is set. The function should return the DEFAULT_PARSER in this scenario as per the defined business logic.
"""

# ********RoostGPT********
import logging
import os
import warnings
import pytest
from cfg_parser import LuigiConfigParser
from toml_parser import LuigiTomlParser
from core import _get_default_parser

class Test_CoreGetDefaultParser:

    @pytest.mark.valid
    def test_with_valid_luigi_config_parser(self):
        os.environ['LUIGI_CONFIG_PARSER'] = 'cfg'
        assert _get_default_parser() == 'cfg'

    @pytest.mark.invalid
    def test_with_invalid_parser(self, caplog):
        os.environ['LUIGI_CONFIG_PARSER'] = 'invalid_parser'
        with pytest.warns(UserWarning):
            assert 'Invalid parser' in caplog.text
            assert _get_default_parser() == 'cfg'

    @pytest.mark.valid
    def test_with_luigi_toml_parser(self):
        os.environ['LUIGI_CONFIG_PARSER'] = 'toml'
        assert _get_default_parser() == 'toml'

    @pytest.mark.valid
    def test_when_luigi_config_parser_is_not_set(self):
        if 'LUIGI_CONFIG_PARSER' in os.environ:
            del os.environ['LUIGI_CONFIG_PARSER']
        assert _get_default_parser() == 'cfg'
