# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=_get_default_parser_da6898e1c5
ROOST_METHOD_SIG_HASH=_get_default_parser_61b2c6fc97


Scenario 1: Test for the default parser when no environment variable is set.
Details:
  TestName: test_default_parser_no_env_variable
  Description: This test is intended to verify the default parser returned when there is no environment variable 'LUIGI_CONFIG_PARSER' set.
Execution:
  Arrange: Unset the 'LUIGI_CONFIG_PARSER' environment variable if it exists. Also, ensure DEFAULT_PARSER and PARSERS are correctly set.
  Act: Call the _get_default_parser function without any parameters.
  Assert: Check if the returned parser is the same as the DEFAULT_PARSER.
Validation:
  This test ensures that the function correctly falls back to the default parser when no environment variable is set. This is critical as it ensures that the system behaves correctly in its default state.

Scenario 2: Test for the correct parser when environment variable is set.
Details:
  TestName: test_correct_parser_with_env_variable
  Description: This test is intended to verify that the function returns the correct parser when the 'LUIGI_CONFIG_PARSER' environment variable is set.
Execution:
  Arrange: Set the 'LUIGI_CONFIG_PARSER' environment variable to a valid parser in PARSERS.
  Act: Call the _get_default_parser function without any parameters.
  Assert: Check if the returned parser is the same as the one set in the 'LUIGI_CONFIG_PARSER' environment variable.
Validation:
  The test ensures that the function correctly reads and returns the parser from the environment variable. This is important as it allows users to dynamically change the parser using the environment variable.

Scenario 3: Test for the default parser when an invalid parser is set in the environment variable.
Details:
  TestName: test_default_parser_with_invalid_env_variable
  Description: This test is intended to verify that the function returns the default parser and raises a warning when an invalid parser is set in the 'LUIGI_CONFIG_PARSER' environment variable.
Execution:
  Arrange: Set the 'LUIGI_CONFIG_PARSER' environment variable to an invalid parser not in PARSERS.
  Act: Call the _get_default_parser function without any parameters.
  Assert: Check if the returned parser is the same as the DEFAULT_PARSER and a warning is raised.
Validation:
  This test ensures that the function correctly handles invalid values in the environment variable by falling back to the default parser and raising a warning. This is important as it prevents the system from breaking due to incorrect configurations.
"""

# ********RoostGPT********
import pytest
import os
import warnings
from core import _get_default_parser
from cfg_parser import LuigiConfigParser
from toml_parser import LuigiTomlParser

class Test_CoreGetDefaultParser:

    @pytest.fixture(autouse=True)
    def set_up(self):
        self.PARSERS = {'cfg': LuigiConfigParser, 'conf': LuigiConfigParser, 'ini': LuigiConfigParser, 'toml': LuigiTomlParser}
        self.DEFAULT_PARSER = 'cfg'

    @pytest.mark.smoke
    def test_default_parser_no_env_variable(self):
        if 'LUIGI_CONFIG_PARSER' in os.environ:
            del os.environ['LUIGI_CONFIG_PARSER']
        assert _get_default_parser() == self.DEFAULT_PARSER

    @pytest.mark.regression
    def test_correct_parser_with_env_variable(self):
        os.environ['LUIGI_CONFIG_PARSER'] = 'toml'
        assert _get_default_parser() == 'toml'

    @pytest.mark.negative
    def test_default_parser_with_invalid_env_variable(self):
        os.environ['LUIGI_CONFIG_PARSER'] = 'invalid_parser'
        with pytest.warns(UserWarning):
            assert _get_default_parser() == self.DEFAULT_PARSER
