# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=get_6fbd4744a7
ROOST_METHOD_SIG_HASH=get_d2f3a08528


```
Scenario 1: Test the function is able to fetch existing section and option
Details:
  TestName: test_get_existing_section_option
  Description: This test verifies if the function is correctly able to fetch an existing section and option from the data.
Execution:
  Arrange: Initialize a data object with known sections and options.
  Act: Invoke the get function with a known section and option.
  Assert: Check if the returned value is as expected.
Validation:
  This test verifies the basic functionality of the get function and is critical to ensure it is correctly fetching data.

Scenario 2: Test the function raises KeyError for non-existing section
Details:
  TestName: test_get_non_existing_section
  Description: This test verifies if the function raises a KeyError when trying to fetch a non-existing section.
Execution:
  Arrange: Initialize a data object with known sections and options.
  Act: Invoke the get function with a non-existing section.
  Assert: Check if a KeyError is raised.
Validation:
  This test is important to ensure that the function correctly handles the case when the requested section does not exist.

Scenario 3: Test the function raises KeyError for non-existing option in existing section
Details:
  TestName: test_get_non_existing_option
  Description: This test verifies if the function raises a KeyError when trying to fetch a non-existing option from an existing section.
Execution:
  Arrange: Initialize a data object with known sections and options.
  Act: Invoke the get function with an existing section but a non-existing option.
  Assert: Check if a KeyError is raised.
Validation:
  This test is important to ensure that the function correctly handles the case when the requested option does not exist in a valid section.

Scenario 4: Test the function returns default value for non-existing section when default is provided
Details:
  TestName: test_get_default_for_non_existing_section
  Description: This test verifies if the function correctly returns a default value when the section does not exist and a default value is provided.
Execution:
  Arrange: Initialize a data object with known sections and options.
  Act: Invoke the get function with a non-existing section and a default value.
  Assert: Check if the returned value is the default value provided.
Validation:
  This test is important to ensure that the function correctly handles the case when the requested section does not exist but a default value is provided.

Scenario 5: Test the function returns default value for non-existing option in existing section when default is provided
Details:
  TestName: test_get_default_for_non_existing_option
  Description: This test verifies if the function correctly returns a default value when the option does not exist in an existing section and a default value is provided.
Execution:
  Arrange: Initialize a data object with known sections and options.
  Act: Invoke the get function with an existing section, a non-existing option, and a default value.
  Assert: Check if the returned value is the default value provided.
Validation:
  This test is important to ensure that the function correctly handles the case when the requested option does not exist in a valid section but a default value is provided.
```
"""

# ********RoostGPT********
import pytest
import os.path
from configparser import ConfigParser
from typing import Any, Dict
from base_parser import BaseParser
from freezing import recursively_freeze
import toml
from toml_parser import LuigiTomlParser

class Test_LuigiTomlParserGet:

    @pytest.mark.valid
    def test_get_existing_section_option(self):
        parser = LuigiTomlParser()
        parser.data = {"section1": {"option1": "value1"}}
        assert parser.get("section1", "option1") == "value1"

    @pytest.mark.invalid
    def test_get_non_existing_section(self):
        parser = LuigiTomlParser()
        parser.data = {"section1": {"option1": "value1"}}
        with pytest.raises(KeyError):
            parser.get("section2", "option1")

    @pytest.mark.invalid
    def test_get_non_existing_option(self):
        parser = LuigiTomlParser()
        parser.data = {"section1": {"option1": "value1"}}
        with pytest.raises(KeyError):
            parser.get("section1", "option2")

    @pytest.mark.valid
    def test_get_default_for_non_existing_section(self):
        parser = LuigiTomlParser()
        parser.data = {"section1": {"option1": "value1"}}
        assert parser.get("section2", "option1", default="default_value") == "default_value"

    @pytest.mark.valid
    def test_get_default_for_non_existing_option(self):
        parser = LuigiTomlParser()
        parser.data = {"section1": {"option1": "value1"}}
        assert parser.get("section1", "option2", default="default_value") == "default_value"
