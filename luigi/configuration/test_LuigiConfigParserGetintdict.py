# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=getintdict_fed097f72d
ROOST_METHOD_SIG_HASH=getintdict_8dcd8d4d77


Scenario 1: Testing when the section has integer values
Details:
  TestName: test_getintdict_with_integer_values
  Description: The test is intended to verify that the function getintdict can correctly parse out and return the dictionary having integer values, excluding those mentioned in 'DEFAULT' section.
Execution:
  Arrange: Instantiate the object and initialize the configurations (both in the section and 'DEFAULT' section) with some integer values.
  Act: Invoke the getintdict function with the section name as parameter.
  Assert: Validate that the function returns a dictionary of expected keys and integer values.
Validation:
  The test validates that the getintdict function can successfully convert the configuration keys to the integer values and exclude those from the 'DEFAULT' section.

Scenario 2: Testing when the section does not contain integer values
Details:
  TestName: test_getintdict_with_non_integer_values
  Description: The test is intended to verify that the function getintdict will throw an appropriate exception when the section values can't be converted into integers.
Execution:
  Arrange: Instantiate the object and initialize the configurations with non-integer values.
  Act: Invoke the getintdict function with the section name as parameter.
  Assert: Check if a ValueError has been raised.
Validation:
  The test verifies that getintdict function can handle and throw an error when invalid integers are provided in the configuration.

Scenario 3: Testing when no section exists in the configuration
Details:
  TestName: test_getintdict_with_no_section
  Description: This test validates the business logic of returning an empty dictionary when the passed section does not exist.
Execution:
  Arrange: Instantiate the object without initializing any sections in the configuration.
  Act: Invoke the getintdict function with a non-existing section name.
  Assert: Validate that the function returns an empty dictionary.
Validation:
  The importance of this test lies in verifying that if no section exists, the function does not fail but instead returns an empty dictionary.

Scenario 4: Testing when the section exists but all keys are in 'DEFAULT'
Details:
  TestName: test_getintdict_with_all_keys_in_default
  Description: This test checks the scenario when all keys are present in 'DEFAULT' section and thus no keys/values are returned in the final dictionary.
Execution:
  Arrange: Instantiate a configuration object where all the keys of a section are also present in 'DEFAULT'.
  Act: Invoke the getintdict function with the section name as parameter.
  Assert: The function should return an empty dictionary as all keys are excluded.
Validation:
  The test validates that the getintdict function excludes keys in 'DEFAULT' section as per business requirements.

"""

# ********RoostGPT********
import os
import re
import warnings
import pytest
from configparser import ConfigParser, NoOptionError, NoSectionError, InterpolationError
from configparser import Interpolation, BasicInterpolation
from base_parser import BaseParser
from cfg_parser import LuigiConfigParser 

class Test_LuigiConfigParserGetintdict:

    @pytest.mark.positive
    def test_getintdict_with_integer_values(self):
        lcp = LuigiConfigParser()
        lcp.add_section("SECTION")
        lcp.set("SECTION", "a", "10")
        lcp.set("SECTION", "b", "20")
        lcp.set("DEFAULT", "c", "30")
        result = lcp.getintdict("SECTION")
        assert result == {"a": 10, "b": 20}

    @pytest.mark.negative
    def test_getintdict_with_non_integer_values(self):
        lcp = LuigiConfigParser()
        lcp.add_section("SECTION")
        lcp.set("SECTION", "key1", "value1")
        lcp.set("SECTION", "key2", "value2")
        with pytest.raises(ValueError):
            lcp.getintdict("SECTION")

    @pytest.mark.negative
    def test_getintdict_with_no_section(self):
        lcp = LuigiConfigParser()
        assert lcp.getintdict("NON_EXISTING_SECTION") == {}

    @pytest.mark.negative
    def test_getintdict_with_all_keys_in_default(self):
        lcp = LuigiConfigParser()
        lcp.add_section("SECTION")
        lcp.set("DEFAULT", "default1", "1")
        lcp.set("DEFAULT", "default2", "2")
        assert lcp.getintdict("SECTION") == {}
