# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=add_config_path_b37d083919
ROOST_METHOD_SIG_HASH=add_config_path_e4357e7904


```
Scenario 1: Test for Non-existence of Config File
Details:
  TestName: test_non_existent_config_file
  Description: This test verifies the function's behavior when the provided path does not correspond to an existing file.
Execution:
  Arrange: Provide a file path that does not exist.
  Act: Invoke the add_config_path function with the non-existent path.
  Assert: The function should return False and a warning should be raised with the message "Config file does not exist: {path}".
Validation:
  This test is important to ensure that the function properly handles the scenario where the provided path does not point to a file. This is a crucial error handling mechanism.

Scenario 2: Test for Unsupported File Extension
Details:
  TestName: test_unsupported_file_extension
  Description: This test checks the function's behavior when the provided file has an unsupported extension.
Execution:
  Arrange: Provide a file path with an unsupported extension.
  Act: Invoke the add_config_path function with the unsupported file path.
  Assert: The function should select the default parser and a warning should be raised indicating that a non-default parser is being used.
Validation:
  This test ensures that the function can handle file types that it wasn't specifically designed to handle, by falling back to a default parser.

Scenario 3: Test for Supported File Extension
Details:
  TestName: test_supported_file_extension
  Description: This test checks the function's behavior when the provided file has a supported extension.
Execution:
  Arrange: Provide a file path with a supported extension.
  Act: Invoke the add_config_path function with the supported file path.
  Assert: The function should select the appropriate parser based on the file extension and add the config path to the parser. The function should return True.
Validation:
  This test ensures that the function correctly identifies the file type based on its extension and uses the appropriate parser to handle it.

Scenario 4: Test for Default Parser Selection
Details:
  TestName: test_default_parser_selection
  Description: This test verifies the function's behavior when no file extension is provided.
Execution:
  Arrange: Provide a file path with no extension.
  Act: Invoke the add_config_path function with the no-extension file path.
  Assert: The function should select the default parser and add the config path to the parser. The function should return True.
Validation:
  This test ensures that the function correctly falls back to the default parser when no file extension is provided.
```
"""

# ********RoostGPT********
import pytest
import os
import warnings
from unittest.mock import patch
from cfg_parser import LuigiConfigParser
from toml_parser import LuigiTomlParser
from core import add_config_path

class Test_CoreAddConfigPath:

    @pytest.mark.error_handling
    def test_non_existent_config_file(self):
        non_existent_path = "/path/does/not/exist.cfg"
        with pytest.raises(Warning) as warning_info:
            result = add_config_path(non_existent_path)
        assert str(warning_info.value) == f"Config file does not exist: {non_existent_path}"
        assert result == False

    @pytest.mark.error_handling
    def test_unsupported_file_extension(self):
        unsupported_file_path = "/path/to/file.unsupported"
        with patch.dict('core.PARSERS', {'cfg': LuigiConfigParser}, clear=True):
            with pytest.raises(Warning) as warning_info:
                result = add_config_path(unsupported_file_path)
            assert "Config for cfg parser added, but used cfg parser." in str(warning_info.value)
            assert result == True

    @pytest.mark.positive
    def test_supported_file_extension(self):
        supported_file_path = "/path/to/file.cfg"
        with patch.dict('core.PARSERS', {'cfg': LuigiConfigParser}, clear=True):
            result = add_config_path(supported_file_path)
            assert result == True

    @pytest.mark.positive
    def test_default_parser_selection(self):
        no_extension_file_path = "/path/to/file"
        with patch.dict('core.PARSERS', {'cfg': LuigiConfigParser}, clear=True):
            result = add_config_path(no_extension_file_path)
            assert result == True
