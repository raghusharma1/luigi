# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=add_config_path_388d3ffdb1
ROOST_METHOD_SIG_HASH=add_config_path_05e54720ac


``` 
Scenario 1: Addition of new path to config paths
Details:
  TestName: test_add_new_path_to_config_paths
  Description: This test is intended to verify if the method correctly adds a new path to the _config_paths list and calls the reload method.
Execution:
  Arrange: Initialize the function with an empty _config_paths list or a list with some existing paths.
  Act: Invoke the function add_config_path with a new path as parameter.
  Assert: Check if the new path is added to the _config_paths list and the reload method is called.
Validation:
  The test ensures that the function correctly updates the _config_paths list with new paths and reloads the configuration. This is important as the function's primary role is to keep the _config_paths list updated.

Scenario 2: Addition of existing path to config paths
Details:
  TestName: test_add_existing_path_to_config_paths
  Description: This test is intended to verify if the method behaves as expected when an existing path is added to the _config_paths list.
Execution:
  Arrange: Initialize the function with a _config_paths list that already includes the path to be added.
  Act: Invoke the function add_config_path with an existing path as parameter.
  Assert: Check if the path is not added again to the _config_paths list and the reload method is called.
Validation:
  The test ensures that the function doesn't add duplicate paths to the _config_paths list. This is important as adding duplicate paths could lead to unnecessary redundancy and potential errors in configuration loading.

Scenario 3: Addition of invalid path to config paths
Details:
  TestName: test_add_invalid_path_to_config_paths
  Description: This test is intended to verify if the method behaves as expected when an invalid path is added to the _config_paths list.
Execution:
  Arrange: Initialize the function with an empty _config_paths list or a list with some existing paths.
  Act: Invoke the function add_config_path with an invalid path as parameter.
  Assert: Check if the invalid path is not added to the _config_paths list and the reload method is not called.
Validation:
  The test ensures that the function correctly handles invalid paths and doesn't add them to the _config_paths list. This is important as adding invalid paths could lead to errors when loading configurations.
```
"""

# ********RoostGPT********
import logging
import pytest
from unittest.mock import patch
from base_parser import BaseParser

class Test_BaseParserAddConfigPath:

    @pytest.mark.positive
    @patch.object(BaseParser, 'reload')
    def test_add_new_path_to_config_paths(self, mock_reload):
        BaseParser._config_paths = ["existing_path"]
        new_path = "new_path"
        BaseParser.add_config_path(new_path)
        assert new_path in BaseParser._config_paths
        mock_reload.assert_called_once()

    @pytest.mark.negative
    @patch.object(BaseParser, 'reload')
    def test_add_existing_path_to_config_paths(self, mock_reload):
        existing_path = "existing_path"
        BaseParser._config_paths = [existing_path]
        BaseParser.add_config_path(existing_path)
        assert BaseParser._config_paths.count(existing_path) == 1
        mock_reload.assert_called_once()

    @pytest.mark.negative
    @patch.object(BaseParser, 'reload')
    def test_add_invalid_path_to_config_paths(self, mock_reload):
        BaseParser._config_paths = ["existing_path"]
        invalid_path = None
        BaseParser.add_config_path(invalid_path)
        assert invalid_path not in BaseParser._config_paths
        mock_reload.assert_not_called()
