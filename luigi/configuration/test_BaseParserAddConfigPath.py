# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=add_config_path_388d3ffdb1
ROOST_METHOD_SIG_HASH=add_config_path_05e54720ac


Scenario 1: Test if config path is added successfully
Details:
  TestName: test_add_config_path_successfully
  Description: This test is validating if the path specified is successfully added to the _config_paths attribute and if the reload method is invoked after the path addition.
Execution:
  Arrange: A dummy class containing the _config_paths attribute and the reload method should be initialized. The _config_paths attribute should be an empty list, and the reload method should perform no operations for this test.
  Act: Call the add_config_path method via the dummy class instance with a sample path as the parameter.
  Assert: Check if the path is successfully appended to the _config_paths list and if the reload method is invoked.
Validation:
  The importance of this test lies in the core functionality of the add_config_path function. Ensuring a path is successfully added and reloaded afterward is crucial for the function's intended behavior.

Scenario 2: Test if multiple config paths are added successfully
Details:
  TestName: test_add_multiple_config_path_successfully
  Description: This test is validating if the method can handle multiple paths being added to it consecutively, and if the reload method is invoked after adding each path.
Execution:
  Arrange: As in the previous scenario, a dummy class containing the _config_paths attribute and the reload method needs to be prepared.
  Act: Invoke the add_config_path method multiple times with different path parameters.
  Assert: Verify if each path is appended to the _config_paths list in the correct order and if the reload method is invoked after adding each path.
Validation:
  This test scenario is significant as it verifies the method's ability to handle multiple operations which is a likely use case in practical scenarios.

Scenario 3: Test if the method functions correctly when called with an empty path
Details:
  TestName: test_add_empty_config_path
  Description: This test checks if the add_config_path method can handle being called with an empty string as a path parameter.
Execution:
  Arrange: Initialize the dummy class with _config_paths attribute and reload method as before.
  Act: Run the add_config_path method with an empty string as the path parameter.
  Assert: Check if the empty string is appended to the _config_paths list and if the reload function is invoked afterward.
Validation:
  This test scenario is important as it checks the method's robustness i.e., its ability to function correctly with less common input values, such as an empty string.
"""

# ********RoostGPT********
import logging
import pytest
from base_parser import BaseParser
from unittest import mock

class Test_BaseParserAddConfigPath:
    @mock.patch.object(BaseParser,'reload')
    @mock.patch.object(BaseParser, '_config_paths', new_callable=list)
    def test_add_config_path_successfully(self, mocked_config_paths, mocked_reload):
        # Arrange
        path = '/dummy/path'
        # Act
        BaseParser.add_config_path(path)
        # Assert
        mocked_reload.assert_called_once()
        assert mocked_config_paths.append.call_args_list == [mock.call(path)]

    @mock.patch.object(BaseParser,'reload')
    @mock.patch.object(BaseParser, '_config_paths', new_callable=list)
    def test_add_multiple_config_path_successfully(self, mocked_config_paths, mocked_reload):
        # Arrange
        path1 = '/dummy/path1'
        path2 = '/dummy/path2'
        # Act
        BaseParser.add_config_path(path1)
        BaseParser.add_config_path(path2)
        # Assert
        assert mocked_config_paths.append.call_args_list == [mock.call(path1), mock.call(path2)]
        assert mocked_reload.call_count == 2

    @mock.patch.object(BaseParser,'reload')
    @mock.patch.object(BaseParser, '_config_paths', new_callable=list)
    def test_add_empty_config_path(self, mocked_config_paths, mocked_reload):
        # Arrange
        empty_path = ''
        # Act
        BaseParser.add_config_path(empty_path)
        # Assert
        mocked_reload.assert_called_once()
        assert mocked_config_paths.append.call_args_list == [mock.call(empty_path)]
