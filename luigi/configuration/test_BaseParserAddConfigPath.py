# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=add_config_path_388d3ffdb1
ROOST_METHOD_SIG_HASH=add_config_path_05e54720ac


Scenario 1: Ensure that the config path is added correctly
Details:
  TestName: test_add_config_path
  Description: This test ensures that the function add_config_path correctly adds a new path to the _config_paths list and reloads the configuration.
Execution:
  Arrange: Initialize an instance of the class with an empty _config_paths list.
  Act: Call the add_config_path function with a valid path string.
  Assert: Check that the _config_paths list now contains the added path and that the reload function has been called.
Validation:
  This test checks that the function correctly adds paths and reloads configurations, which is crucial for ensuring that the application can access and use updated configuration information.

Scenario 2: Ensure that multiple config paths are added in the correct order
Details:
  TestName: test_order_of_added_config_paths
  Description: This test verifies that multiple config paths are added to the _config_paths list in the order they were added.
Execution:
  Arrange: Initialize an instance of the class with an empty _config_paths list.
  Act: Call the add_config_path function multiple times with different path strings.
  Assert: Check that the _config_paths list contains the added paths in the order they were added and that the reload function has been called after each addition.
Validation:
  This test ensures that the order of added paths is maintained, which could be vital if the order of configuration paths matters for the application's functionality.

Scenario 3: Ensure that the same config path is not added multiple times
Details:
  TestName: test_no_duplicate_config_paths
  Description: This test checks that if the same path is added multiple times, it only appears once in the _config_paths list.
Execution:
  Arrange: Initialize an instance of the class with an empty _config_paths list.
  Act: Call the add_config_path function multiple times with the same path string.
  Assert: Check that the _config_paths list only contains one instance of the added path and that the reload function has been called each time.
Validation:
  This test ensures that the function does not add duplicate paths, which could cause unnecessary reloading of configurations and potentially slow down the application.
"""

# ********RoostGPT********
import logging
import pytest
from base_parser import BaseParser
from unittest.mock import MagicMock, call

class Test_BaseParserAddConfigPath:
    
    @pytest.mark.regression
    def test_add_config_path(self):
        # Arrange
        BaseParser._config_paths = []
        BaseParser.reload = MagicMock()
        path = "valid/path"

        # Act
        BaseParser.add_config_path(path)

        # Assert
        assert BaseParser._config_paths == [path]
        BaseParser.reload.assert_called_once()

    @pytest.mark.regression
    def test_order_of_added_config_paths(self):
        # Arrange
        BaseParser._config_paths = []
        BaseParser.reload = MagicMock()
        paths = ["valid/path1", "valid/path2", "valid/path3"]

        # Act
        for path in paths:
            BaseParser.add_config_path(path)

        # Assert
        assert BaseParser._config_paths == paths
        assert BaseParser.reload.call_count == len(paths)

    @pytest.mark.regression
    def test_no_duplicate_config_paths(self):
        # Arrange
        BaseParser._config_paths = []
        BaseParser.reload = MagicMock()
        path = "valid/path"

        # Act
        BaseParser.add_config_path(path)
        BaseParser.add_config_path(path)

        # Assert
        assert BaseParser._config_paths == [path]
        assert BaseParser.reload.call_count == 2
