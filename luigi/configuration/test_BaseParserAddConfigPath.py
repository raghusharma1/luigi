# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=add_config_path_388d3ffdb1
ROOST_METHOD_SIG_HASH=add_config_path_05e54720ac


Scenario 1: Testing the addition of a valid path
Details:
  TestName: test_add_valid_config_path
  Description: This test is intended to verify if a valid path can be added to the _config_paths list and whether the reload function is called afterward.
Execution:
  Arrange: Initialize the _config_paths list with some initial paths. Also, mock the reload method to prevent actual reloading and to check if it gets called.
  Act: Invoke the add_config_path function with a valid path.
  Assert: Check if the path was correctly added to the _config_paths list and if the reload function was called.
Validation:
  This test ensures that the function correctly handles the addition of new paths and triggers the necessary reloading of configuration data.

Scenario 2: Testing the addition of a duplicate path
Details:
  TestName: test_add_duplicate_config_path
  Description: This test is intended to verify how the function behaves when a path that already exists in the _config_paths list is added.
Execution:
  Arrange: Initialize the _config_paths list with some paths, including the one that will be added again. Mock the reload method to prevent actual reloading.
  Act: Invoke the add_config_path function with a path that already exists in the _config_paths list.
  Assert: Check if the path is not added again to the _config_paths list and if the reload function was called.
Validation:
  This test ensures that the function does not add duplicate paths, which would unnecessarily increase memory usage and possibly lead to redundant reloading of configuration data.

Scenario 3: Testing the addition of an invalid path
Details:
  TestName: test_add_invalid_config_path
  Description: This test is intended to verify how the function behaves when an invalid path is added.
Execution:
  Arrange: Initialize the _config_paths list with some paths. Mock the reload method to prevent actual reloading.
  Act: Invoke the add_config_path function with an invalid path.
  Assert: Check if the path was not added to the _config_paths list and if the reload function was not called.
Validation:
  This test ensures that the function correctly handles invalid paths, preventing them from being added to the _config_paths list and from triggering unnecessary reloading of configuration data.
"""

# ********RoostGPT********
import logging
import pytest
from base_parser import BaseParser
from unittest.mock import MagicMock

class Test_BaseParserAddConfigPath:
    
    @pytest.mark.valid
    def test_add_valid_config_path(self):
        # Arrange
        BaseParser._config_paths = ['path1', 'path2']
        BaseParser.reload = MagicMock()
        new_path = 'path3'

        # Act
        BaseParser.add_config_path(new_path)

        # Assert
        assert new_path in BaseParser._config_paths
        BaseParser.reload.assert_called_once()

    @pytest.mark.duplicate
    def test_add_duplicate_config_path(self):
        # Arrange
        BaseParser._config_paths = ['path1', 'path2']
        BaseParser.reload = MagicMock()
        duplicate_path = 'path1'

        # Act
        BaseParser.add_config_path(duplicate_path)

        # Assert
        assert BaseParser._config_paths.count(duplicate_path) == 1
        BaseParser.reload.assert_called_once()

    @pytest.mark.invalid
    def test_add_invalid_config_path(self):
        # Arrange
        BaseParser._config_paths = ['path1', 'path2']
        BaseParser.reload = MagicMock()
        invalid_path = ''

        # Act
        BaseParser.add_config_path(invalid_path)

        # Assert
        assert invalid_path not in BaseParser._config_paths
        BaseParser.reload.assert_not_called()
