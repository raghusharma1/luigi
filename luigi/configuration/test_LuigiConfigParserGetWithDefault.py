# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=_get_with_default_aeb85b75c1
ROOST_METHOD_SIG_HASH=_get_with_default_19a36c0a1b


Scenario 1: Test when the option is found using underscore-style
Details:
  TestName: test_get_with_default_option_found_underscore_style
  Description: This test verifies that the function correctly retrieves the value of the option when it is found using underscore-style.
Execution:
  Arrange: Initialize the object with a section and option in underscore-style. Prepare a mock method that returns a value when called with the section and option.
  Act: Call _get_with_default with the mock method, section, and option.
  Assert: Check that the return value is the same as the one returned by the mock method.
Validation:
  This test is important to ensure that the function correctly supports the recommended configuration style (underscore-style).

Scenario 2: Test when the option is not found in underscore-style but found in dash-style
Details:
  TestName: test_get_with_default_option_found_dash_style
  Description: This test verifies that the function correctly retrieves the value of the option when it is not found in underscore-style but found in dash-style.
Execution:
  Arrange: Initialize the object with a section and option in dash-style. Prepare a mock method that raises a NoOptionError when called with the section and option in underscore-style, and returns a value when called with the section and option in dash-style.
  Act: Call _get_with_default with the mock method, section, and option.
  Assert: Check that the return value is the same as the one returned by the mock method when called with the dash-style option. Also, verify that a DeprecationWarning is issued.
Validation:
  This test is important to ensure that the function correctly supports dash-style option names with a deprecation warning.

Scenario 3: Test when the option is not found and a default value is returned
Details:
  TestName: test_get_with_default_option_not_found_return_default
  Description: This test verifies that the function correctly returns the default value when the option is not found.
Execution:
  Arrange: Initialize the object without the section and option. Prepare a mock method that raises a NoOptionError when called. Also, prepare a default value.
  Act: Call _get_with_default with the mock method, section, option, and the default value.
  Assert: Check that the return value is the same as the default value.
Validation:
  This test is important to ensure that the function correctly returns the default value when the option is not found.

Scenario 4: Test when the default value doesn't match the expected type
Details:
  TestName: test_get_with_default_default_value_wrong_type
  Description: This test verifies that the function correctly raises an exception when the default value does not match the expected type.
Execution:
  Arrange: Initialize the object without the section and option. Prepare a mock method that raises a NoOptionError when called. Also, prepare a default value and an expected type that doesn't match the type of the default value.
  Act: Call _get_with_default with the mock method, section, option, the default value, and the expected type.
  Assert: Check that an exception is raised.
Validation:
  This test is important to ensure that the function correctly checks the type of the default value and raises an exception when it doesn't match the expected type.
"""

# ********RoostGPT********
import os
import re
import warnings
import pytest
from configparser import ConfigParser, NoOptionError, NoSectionError, InterpolationError
from configparser import Interpolation, BasicInterpolation
from base_parser import BaseParser
from cfg_parser import _get_with_default
from unittest.mock import MagicMock


class Test_LuigiConfigParserGetWithDefault:
    def test_get_with_default_option_found_underscore_style(self):
        mock_method = MagicMock(return_value='value')
        section = 'section'
        option = 'option'

        result = _get_with_default(mock_method, section, option, default=None)

        mock_method.assert_called_once_with(section, option)
        assert result == 'value'

    def test_get_with_default_option_found_dash_style(self):
        mock_method = MagicMock(side_effect=[NoOptionError('option', 'section'), 'value'])
        section = 'section'
        option = 'option'

        with warnings.catch_warnings(record=True) as w:
            warnings.simplefilter("always")
            result = _get_with_default(mock_method, section, option, default=None)
            assert len(w) == 1
            assert issubclass(w[-1].category, DeprecationWarning)

        mock_method.assert_called_with(section, option.replace('_', '-'))
        assert result == 'value'

    def test_get_with_default_option_not_found_return_default(self):
        mock_method = MagicMock(side_effect=NoOptionError('option', 'section'))
        section = 'section'
        option = 'option'
        default = 'default'

        result = _get_with_default(mock_method, section, option, default=default)

        assert result == default

    def test_get_with_default_default_value_wrong_type(self):
        mock_method = MagicMock(side_effect=NoOptionError('option', 'section'))
        section = 'section'
        option = 'option'
        default = 'default'

        with pytest.raises(TypeError):
            _get_with_default(mock_method, section, option, default=default, expected_type=int)
