# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=_get_with_default_aeb85b75c1
ROOST_METHOD_SIG_HASH=_get_with_default_19a36c0a1b


Scenario 1: Option name contains hyphen
Details:
  TestName: test_option_name_with_hyphen
  Description: This test is intended to verify that when an option name containing a hyphen is passed, the function replaces the hyphen with an underscore and returns the appropriate value.
Execution:
  Arrange: Initialize the method with a section, option (containing a hyphen), default value, and expected type.
  Act: Invoke the function _get_with_default with the initialized values.
  Assert: Check if the function returns the correct value and issues a deprecation warning.
Validation:
  This test is important to ensure that the function handles option names with hyphens correctly and issues a deprecation warning as per business requirements.

Scenario 2: Option or section not found
Details:
  TestName: test_option_or_section_not_found
  Description: This test is intended to verify that the function returns the default value when the given option or section is not found.
Execution:
  Arrange: Initialize the method with a non-existing section or option, a default value, and expected type.
  Act: Invoke the function _get_with_default with the initialized values.
  Assert: Check if the function returns the default value.
Validation:
  This test is critical to ensure that the function gracefully handles scenarios where a section or option is not found and returns the default value as per the business requirement.

Scenario 3: Default value type mismatch
Details:
  TestName: test_default_value_type_mismatch
  Description: This test is designed to verify that the function raises an exception when the default value's type doesn't match the expected type.
Execution:
  Arrange: Initialize the method with a section, option, default value (of a different type than expected), and expected type.
  Act: Invoke the function _get_with_default with the initialized values.
  Assert: Check if the function raises an exception due to type mismatch.
Validation:
  This test is important to ensure that the function correctly validates the type of default value and raises an exception when a mismatch is detected, as per the business requirements.

Scenario 4: No default value provided
Details:
  TestName: test_no_default_value
  Description: This test is intended to verify that the function raises an exception when no default value is provided, and the section or option does not exist.
Execution:
  Arrange: Initialize the method with a non-existing section or option and no default value.
  Act: Invoke the function _get_with_default with the initialized values.
  Assert: Check if the function raises an exception due to the absence of a default value.
Validation:
  This test is essential to ensure that the function correctly handles scenarios where no default value is provided and raises an exception when a section or option is not found, as per the business requirements.
"""

# ********RoostGPT********
import os
import re
import warnings
import pytest
from configparser import ConfigParser, NoOptionError, NoSectionError, InterpolationError
from configparser import Interpolation, BasicInterpolation
from base_parser import BaseParser
from cfg_parser import _get_with_default

class Test_LuigiConfigParserGetWithDefault:

    @pytest.mark.smoke
    def test_option_name_with_hyphen(self):
        # Arrange
        section = 'test_section'
        option = 'test-option'
        default = 'default_value'
        expected_type = str
        expected_value = 'correct_value'
        # Replace '-' with '_' in option
        option_modified = option.replace('-', '_')

        # Act
        value = _get_with_default(ConfigParser.get, section, option, default, expected_type)

        # Assert
        assert value == expected_value
        with pytest.warns(DeprecationWarning):
            _get_with_default(ConfigParser.get, section, option, default, expected_type)

    @pytest.mark.regression
    def test_option_or_section_not_found(self):
        # Arrange
        section = 'non_existing_section'
        option = 'non_existing_option'
        default = 'default_value'
        expected_type = str

        # Act
        value = _get_with_default(ConfigParser.get, section, option, default, expected_type)

        # Assert
        assert value == default

    @pytest.mark.invalid
    def test_default_value_type_mismatch(self):
        # Arrange
        section = 'test_section'
        option = 'test_option'
        default = 100  # int type
        expected_type = str  # expected str type

        # Act & Assert
        with pytest.raises(TypeError):
            _get_with_default(ConfigParser.get, section, option, default, expected_type)

    @pytest.mark.negative
    def test_no_default_value(self):
        # Arrange
        section = 'non_existing_section'
        option = 'non_existing_option'
        default = None
        expected_type = None

        # Act & Assert
        with pytest.raises(NoOptionError):
            _get_with_default(ConfigParser.get, section, option, default, expected_type)
