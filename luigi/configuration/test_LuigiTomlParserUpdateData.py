# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonBigProjectTest using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=_update_data_15ecc0721f
ROOST_METHOD_SIG_HASH=_update_data_7cc237bafe


```
Scenario 1: Test when both data and new_data are not empty
Details:
  TestName: test_update_data_with_both_data_and_new_data
  Description: This test verifies that the function correctly updates the existing data with new_data and returns the updated data.
Execution:
  Arrange: Prepare data and new_data with non-empty dictionaries.
  Act: Invoke _update_data with prepared data and new_data.
  Assert: Check if the returned data is the updated version of the previous data.
Validation:
  This test ensures that the function correctly handles the scenario when both data and new_data are provided. This is crucial as the main purpose of the function is to update the existing data with new_data.

Scenario 2: Test when data is not empty and new_data is empty
Details:
  TestName: test_update_data_with_empty_new_data
  Description: This test verifies that the function correctly handles the scenario when new_data is empty.
Execution:
  Arrange: Prepare data with non-empty dictionary and new_data with an empty dictionary.
  Act: Invoke _update_data with prepared data and new_data.
  Assert: Check if the returned data is the same as the original data.
Validation:
  This test ensures that the function correctly handles the scenario when new_data is empty. This is important as it verifies the function's ability to return the original data when no new data is provided to update it.

Scenario 3: Test when data is empty and new_data is not empty
Details:
  TestName: test_update_data_with_empty_data
  Description: This test verifies that the function correctly handles the scenario when data is empty.
Execution:
  Arrange: Prepare data with an empty dictionary and new_data with a non-empty dictionary.
  Act: Invoke _update_data with prepared data and new_data.
  Assert: Check if the returned data is the same as new_data.
Validation:
  This test ensures that the function correctly handles the scenario when data is empty. This is important as it verifies the function's ability to return new_data when no original data is provided.

Scenario 4: Test when both data and new_data are empty
Details:
  TestName: test_update_data_with_both_empty
  Description: This test verifies that the function correctly handles the scenario when both data and new_data are empty.
Execution:
  Arrange: Prepare data and new_data with empty dictionaries.
  Act: Invoke _update_data with prepared data and new_data.
  Assert: Check if the returned data is an empty dictionary.
Validation:
  This test ensures that the function correctly handles the scenario when both data and new_data are empty. This is important as it checks the function's ability to return an empty dictionary when no data is provided to it.
```

"""

# ********RoostGPT********
import os.path
from configparser import ConfigParser
from typing import Any, Dict
from base_parser import BaseParser
from freezing import recursively_freeze
import toml
import pytest
from toml_parser import _update_data

class Test_LuigiTomlParserUpdateData:
    @pytest.mark.valid
    def test_update_data_with_both_data_and_new_data(self):
        # Arrange
        data = {'section1': {'key1': 'value1'}}
        new_data = {'section1': {'key2': 'value2'}}
        
        # Act
        result = _update_data(data, new_data)
        
        # Assert
        assert result == {'section1': {'key1': 'value1', 'key2': 'value2'}}

    @pytest.mark.valid
    def test_update_data_with_empty_new_data(self):
        # Arrange
        data = {'section1': {'key1': 'value1'}}
        new_data = {}
        
        # Act
        result = _update_data(data, new_data)
        
        # Assert
        assert result == data

    @pytest.mark.valid
    def test_update_data_with_empty_data(self):
        # Arrange
        data = {}
        new_data = {'section1': {'key1': 'value1'}}
        
        # Act
        result = _update_data(data, new_data)
        
        # Assert
        assert result == new_data

    @pytest.mark.valid
    def test_update_data_with_both_empty(self):
        # Arrange
        data = {}
        new_data = {}
        
        # Act
        result = _update_data(data, new_data)
        
        # Assert
        assert result == {}
